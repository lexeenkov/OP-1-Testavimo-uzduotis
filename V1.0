#include <iostream>
#include <string>

using std::cin;
using std::getline;
using std::cout;
using std::endl;
using std::string;

int main() {
	string eil1;
	string eil3;
	string vard;
	string lyt;
	int n;
	int w;

	cout << "Jusu vardas: ";
	getline(cin, vard);

	cout << "Jusu lytis (V / M): ";
	getline(cin, lyt);
	tryagain:
	cout << "Korteles plotis (min. 5 eilutes): ";
	cin >> w;
	

	if (w < 5) {
		cout << endl << "Per mazas plotis." << endl;
		goto tryagain;
	}
	
	if (w%2==0) {
		cout << endl << "Netinkamas plotis , iveskite nelygini skaiciu." << endl;
		goto tryagain;
	}

	if (lyt == "M" || lyt == "m") {
		eil3 = "* Sveika , " + vard + "! *";
	}
	else {
		eil3 = "* Sveikas , " + vard + "! *";
	};

	n = eil3.size();

	for (int i = 0; i < n; i++) {
		eil1 = eil1 + "*";

	};
	for (int t = 0; t < (w-3)/2 ; t++) {
		eil1 = eil1 + "\n*";
		for (int i = 0; i < (n - 2); i++) {
			eil1 = eil1 + " ";
		}
		eil1 = eil1 + "*";
	};

		

		cout << endl << eil1 << endl;
		cout << eil3 << endl;
		reverse(eil1.begin(), eil1.end());
		cout << eil1 << endl;

		return 0;
	
}

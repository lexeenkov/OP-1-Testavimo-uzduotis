#include <iostream>
#include <string>

using std::cin;
using std::cout;
using std::endl;
using std::string;

int main(){
	string eil1;
	string eil2="*";
	string eil3;
	string vard;
	int n;

	cout << "Input your name: ";
	cin >> vard;

	if (vard.substr(vard.length() - 1) == "a"){
		eil3 = "* Sveika , " + vard + "! *";
	}
	else {
		eil3 = "* Sveikas , " + vard + "! *";
	};

	n = eil3.size();
	
	for (int i = 0; i < n; i++) {
		eil1 = eil1 + "*";
		eil2 = eil2 + " ";
	}

	eil2.erase(15,2);
	eil2 = eil2 + "*";


	cout << endl << eil1 << endl;
	cout << eil2 << endl;
	cout << eil3 << endl;
	cout << eil2 << endl;
	cout << eil1 << endl;

	return 0;
}
